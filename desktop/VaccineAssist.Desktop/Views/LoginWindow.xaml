<Window x:Class="VaccineAssist.Desktop.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Vaccine Assist — Sign in"
        Width="380" Height="320"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Vaccine Assist" FontSize="20" FontWeight="Bold"/>

        <TextBlock Grid.Row="2" Text="Shared pharmacy email"/>
        <TextBox Grid.Row="3" x:Name="EmailTextBox" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="4" Text="Password"/>
        <PasswordBox Grid.Row="5" x:Name="PasswordInput"/>

        <Button Grid.Row="6" x:Name="SignInButton" Content="Sign in" Height="32"
                Click="SignInButton_OnClick"/>

        <TextBlock Grid.Row="8" Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap"/>

        <TextBlock Grid.Row="10" Text="No Supabase project yet? Set SupabaseUrl/SupabaseAnonKey in %AppData%\VaccineAssist\settings.json."
                    TextWrapping="Wrap" FontSize="10" Foreground="Gray"/>
    </Grid>
</Window>
